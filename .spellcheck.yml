matrix:
- name: golang
  sources:
  - '**/*.go'
  filters:
  - pyspelling.filters.cpp:
      group_comments: true
      strip_comments: true

- name: markdown
  sources:
  - '**/*.md'
  filters:
  - pyspelling.filters.markdown:
  - pyspelling.filters.html:
      comments: false
      attributes:
      - title
      - alt
      ignores:
      - code
      - pre
